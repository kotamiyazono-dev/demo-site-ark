name: Assign Copilot on Approval

on:
  issues:
    types: [labeled]

jobs:
  assign-copilot:
    if: |
      github.event.label.name == 'approved-copilot'
    runs-on: ubuntu-latest
    
    permissions:
      issues: write
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Assign Copilot and add comment
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.COPILOT_PAT }}
          script: |
            try {
              const issue = await github.rest.issues.get({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number
              });
              
              const issueNodeId = issue.data.node_id;
              const copilotNodeId = 'BOT_kgDOC9w8XQ';
              
              const mutation = `
                mutation {
                  addAssigneesToAssignable(input: {
                    assignableId: "${issueNodeId}",
                    assigneeIds: ["${copilotNodeId}"]
                  }) {
                    assignable {
                      ... on Issue {
                        assignees(first: 10) {
                          nodes {
                            login
                          }
                        }
                      }
                    }
                  }
                }
              `;
              
              await github.graphql(mutation, {
                headers: {
                  'GraphQL-Features': 'issues_copilot_assignment_api_support'
                }
              });
              
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: '🤖 Copilot Workspace に割り当てました。処理を開始します。'
              });
            } catch (error) {
              console.error(error);
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: '⚠️ Copilot の自動アサインに失敗しました。管理者が手動で対応してください。'
              });
              throw error;
            }
